create table ypTeacher
(ID int primary key,
teacherID int references teacher(ID),
year int,
yearAmount int);

create table mpTeacher
(ID int primary key,
teacherID int references teacher(ID),
month int,
monthAmount int);

create table ypClass
(ID int primary key,
classID int references class(ID),
year int,
yearAmount int);

create table mpClass
(ID int primary key,
classID int references class(ID),
month int,
monthAmount int);

create table studentAnalysis
(classID int primary key references class(ID),
averageAge numeric,
job nvarchar(50));


